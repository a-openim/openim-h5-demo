apiVersion: v1
kind: Service
metadata:
  name: h5-openim
  labels:
    app: h5-openim
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: h5-openim
